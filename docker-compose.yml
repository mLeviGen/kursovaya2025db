services:
  db:
    image: postgres:16
    container_name: cheese-db
    env_file: ./db-postgresql/.env
    ports:
      - "${POSTGRES_PORT:-5433}:5433"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db-postgresql/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 3s
      timeout: 3s
      retries: 30

  migrator:
    image: postgres:16
    depends_on:
      db:
        condition: service_healthy
    env_file: ./db-postgresql/.env
    entrypoint: ["/bin/bash", "-lc"]
    command: "bash /sql/migrate.sh"
    volumes:
      - ./db-postgresql/migrate.sh:/sql/migrate.sh:ro
      - ./db-postgresql/migrations:/sql/migrations:ro
      - ./db-postgresql/views:/sql/views:ro
      - ./db-postgresql/triggers:/sql/triggers:ro
    restart: "no"

volumes:
  pgdata:
